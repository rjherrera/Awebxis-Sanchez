<h1>Interest on user's books</h1>

<ul>
  <% interests.forEach(interest => { %>
    <li>
      · <%= interest.book.title %> <span class="interest-count">- <%= stats.interestsCount %> Interesados</span>
    </li>
  <% }) %>
</ul>
